Dependencies:

  Ruby
  MongoDB
  Nginx

Install Gems:

  bundle install

Configure Nginx and reload it:

  cp config/nginx.conf.example /path/to/nginx/conf/conf.d/fakebook
  vi /path/to/nginx/conf/conf.d/fakebook
  sudo /path/to/nginx/sbin/nginx -s reload

Start Thin and DelayedJob:

  foreman start -p 3000
